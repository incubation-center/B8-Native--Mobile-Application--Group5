import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ProductAchivescreen extends StatelessWidget {
  const ProductAchivescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Product Achive Screen.!!!!"),
      ),
    );
  }
}
